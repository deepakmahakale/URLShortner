<%= form_for(@url) do |f| %>

  <div class="field">
    <%= f.text_field :target_url %>
    <%= f.submit 'Create Short URL' %>
  </div>
  <% if @url.errors.any? %>
    <div class="error"><%= @url.errors.full_messages.join %></div>
  <% end %>
<% end %>
